# 01 — Blink LED

Этот проект демонстрирует самый базовый и самый важный принцип работы Arduino — управление цифровым выходом. Мы используем пин **D8**, резистор **220 Ом** и обычный светодиод.

## 📦 Оборудование

* Arduino UNO R3
* Светодиод (любой цвет)
* Резистор 220 Ом
* Макетная плата
* Провода (папа–папа)

## 🔌 Схема подключения

```
Arduino D8 -----> [Резистор 220 Ом] -----> (Длинная ножка +) LED (- короткая ножка) -----> GND
```

### Объяснение

* **Длинная ножка LED** — это **анод (+)**
* **Короткая ножка** — это **катод (-)**, идёт в GND
* Резистор ставится **в разрыв цепи**, чтобы ограничить ток и не сжечь светодиод

## 📄 Код (blink_led.ino)

```
constexpr int kLedPin = 8;        
constexpr unsigned long kOnMs = 500;
constexpr unsigned long kOffMs = 500;

void setup() {
  pinMode(kLedPin, OUTPUT);
  digitalWrite(kLedPin, LOW);
}

void loop() {
  digitalWrite(kLedPin, HIGH);
  delay(kOnMs);
  digitalWrite(kLedPin, LOW);
  delay(kOffMs);
}
```

## 🧠 Как это работает

* `pinMode(8, OUTPUT)` — говорит Arduino, что пин 8 работает как выход.
* `digitalWrite(8, HIGH)` — подаёт +5В → LED загорается.
* `digitalWrite(8, LOW)` — выключает.
* `delay(500)` — делает паузу в миллисекундах.

Этот принцип используется **во всех** проектах Arduino — управление моторами, сервами, RGB, реле и т.д.

## ✅ Что ты освоил

* работу с цифровым выходом;
* базовое подключение светодиода;
* ограничение тока резистором;
* структуру Arduino-программы: `setup()` и `loop()`;
* первый коммит и пуш проекта в GitHub.

---

## 🖼 Фото проекта

<div align="center">

  <![blink-scheme](https://github.com/user-attachments/assets/0a21f651-4ba5-485a-bd0f-14adb75e4485)>


  <img src="img/blink-setup.jpg" alt="Blink LED Setup" width="450" />


---

## 🔧 ASCII-схема подключения 

Ниже — аккуратная ASCII-диаграмма, чтобы понять принцип без фотографии:

```
                    +5V
                     |
                     |
                  [   ] 220Ω резистор
                     |
Arduino D8 o----------*------------------->| LED
                                          ( )   
                                          ( )
                                          ( )
                               Короткая ножка |
                                              |
Arduino GND o----------------------------------*
```

Легенда:

* `>|` — диод, стрелка указывает направление тока
* длинная ножка — **анод (+)** → к резистору → к D8
* короткая ножка — **катод (-)** → к GND

---

